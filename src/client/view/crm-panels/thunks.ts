// import { CRM_PANELS } from "./constants";
// import { createAsyncThunk } from "@reduxjs/toolkit";

// export const some = createAsyncThunk<any, any, any>(
//   `${SOMEFETCH}/fetch`,
//   async (_, { rejectWithValue }) => {
//     try {
//       api,
//     } catch (error) {
//       return rejectWithValue(error as string);
//     }
//   }
// );
